fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.1
pydantic[email]==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
fastapi-mail==1.4.1
openai==1.30.0
langchain==0.2.0
langchain-community==0.2.0
langchain-openai==0.1.0
langchain-ollama          # Local LLMs via Ollama (Batch 8: Local source)
pypdf==4.2.0
tiktoken==0.7.0
pdfplumber==0.11.0
pgvector==0.3.0
# ── LlamaIndex (Loaders & Parsers) ──
llama-index-core>=0.12.0
llama-index-readers-file>=0.4.0
llama-index>=0.12.0
 
# ── Required by loaders (some may already be installed) ──
pandas>=2.0
openpyxl>=3.1.0
tabulate>=0.9.0
beautifulsoup4>=4.12
chardet>=5.0
requests>=2.31
unstructured[pdf,docx,md,xlsx,csv]

# ── Document extraction (Docling loaders/parsers + fast PyMuPDF path) ──
docling>=2.9
pymupdf>=1.23
python-pptx>=1.0       # PPTX: Docling's backend + native fallback loader
markdown-it-py>=3.0    # CommonMark + GFM markdown parsing (element schema)
lxml>=5.0              # XML tree parsing (element schema)
ks-xlsx-parser>=0.2    # RAG-native Excel parsing (chunks + citations; openpyxl fallback)

# ── Web parsing ──
crawl4ai>=0.9          # AI/RAG web crawler (JS rendering + markdown); requests fallback
feedparser>=6.0        # RSS/Atom feed parsing

# ── Cleaning pipeline ──
ftfy>=6.0
clean-text>=0.6
Unidecode>=1.3
langdetect>=1.0

# ── Vision image summarization (multi-vector RAG) ──
langchain-google-genai>=1.0